<script>$('body').attr('id', 'organizer-event-orders').addClass('organizer-event-page');</script>

<%= render partial: "layouts/parts/nav" %>
<script>
  $('#organizer-nav #menu-event-list').addClass('active');
  $('.organizer-event-nav .organizer-event-nav-orders').addClass('active');
</script>

<div class="section" id="order-list-section">
  <div class="container">
    <div class="section-header row">
      <div class="col-xs-12">
        <h1 class="event-title"><%= @event.title %></h1>
      </div>
    </div>
    <div class="section-body row">
      <div class="col-xs-12 col-md-2">
        <%= render partial: "organizer/parts/organizer-event-nav" %>
      </div>
      <div class="col-xs-12 col-md-10">
        <div class="event-section-panel">
          <div class="panel-header">
            <h3>ALL ORDERS</h3>
            <ul class="list-inline">
              <li>Orders: <%= @event.orders.paid.count %></li>
              <li>Total Sales: <%= convert_to_currency @event.get_total_sales %></li>
              <li>Attendees: <%= @event.tickets.paid.count %></li>
            </ul>
          </div>
          <div class="table-responsive organizer-table">
            <table class="table table-hover">
              <thead>
                <tr>
                  <td>ORDER #</td>
                  <td>TICKET BUYER</td>
                  <td>QUANTITY</td>
                  <td>PRICE</td>
                  <td>PURCHASED DATE</td>
                  <td>PAYMENT</td>
                </tr>
              </thead>
              <tbody>
                <% @orders.each do |order| %>
                <tr>
                  <td><%= order.code %></td>
                  <td><%= order.user.first_name %> <%= order.user.last_name %></td>
                  <td><%= order.tickets.count %></td>
                  <td><%= convert_to_currency(order.to_price) %></td>
                  <td><%= order.created_at.strftime("%d %B %Y, %H:%M") %></td>
                  <td>
                    <span class="order-status <%= order.status %>"><%= order.status %></span>
                    <% if !order.payment.nil? %>
                      - <%= order.payment.channel %>
                    <% end %>
                  </td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
