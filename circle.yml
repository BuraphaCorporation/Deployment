machine:
  timezone:
    Asia/Bangkok
  ruby:
    version:
      2.3.1

dependencies:
  pre:
    - gem install bundler -v 1.13.6
  override:
    - bundle install
  post:
    - bundle exec rake assets:precompile
