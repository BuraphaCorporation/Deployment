machine:
  timezone:
    Asia/Bangkok
  ruby:
    version:
      2.4.0
  node:
    version:
      7.3.0

dependencies:
  pre:
    - gem install bundler -v 1.13.7
    # - npm install webpack
  override:
    - bundle install
    - npm install
  post:
    - bundle exec rake assets:precompile
