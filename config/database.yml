
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: daydash_development

test:
  <<: *default
  database: daydash_test

server: &server
  adapter: postgresql
  encoding: unicode
  database: daydash_production
  username: daydash
  password: <%= ENV['DATABASE_PASSWORD'] %>
  host: localhost
  port: 5432

alpha:
  <<: *server
production:
  <<: *server
